//
// Created by marck on 24/11/2021.
//

#include <stdlib.h>
#include <iostream>
#include <time.h>
#include <vector>
#include <cmath>

using namespace std;

int modulo(int, int );
int E_recursivo(int, int );
int Euclides(int, int );
int mcd(int, int);
int E_extendido(int, int );
int Exponenciacion_mod(int, int, int );
int random(int begin = 0, int ind = 100);
vector<int> criba_eratostenes(int);
vector <int> prime_bits(int);
